# Copy to .env.production for store-ready production builds (plasmo build / plasmo package).
# Inlined into the extension bundle at build time — not a secret, just a build-time constant.
PLASMO_PUBLIC_API_URL=https://metaspend.app/api/v1
